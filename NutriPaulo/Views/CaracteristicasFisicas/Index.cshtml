@model IEnumerable<NutriPaulo.Models.CaracteristicasFisicas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Altura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Peso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Biotipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PessoaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Altura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Peso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Biotipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PessoaId.PessoasId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CaracteFisicasId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CaracteFisicasId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CaracteFisicasId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
